apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    name: web
    app: php-app
spec:
  selector:
    name: web 
  type: NodePort
  ports:
   - name: http
     port: 8080
     targetPort: 80
     nodePort: 30001
     protocol: TCP